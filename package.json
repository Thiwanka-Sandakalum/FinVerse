{
    "name": "finverse",
    "version": "0.0.1",
    "private": true,
    "engines": {
        "node": ">=16.0.0"
    },
    "scripts": {
        "build": "pnpm --filter=* build",
        "dev": "pnpm --filter=* dev",
        "lint": "pnpm --filter=* lint",
        "test": "pnpm --filter=* test"
    },
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "devDependencies": {
        "typescript": "^5.0.0",
        "eslint": "^8.38.0"
    }
}